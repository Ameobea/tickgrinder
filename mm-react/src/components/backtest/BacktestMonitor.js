//! Lists running backtests and has options for stopping them, monitoring their progress, etc.

import React from 'react';
import { connect } from 'dva';
import { Table } from 'antd';

const BacktestMonitor = ({dispatch}) => {
  return (

  );
};

export default connect()(BacktestMonitor);
