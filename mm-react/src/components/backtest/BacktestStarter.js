//! Component to initialize backtests

import React from 'react';
import { connect } from 'dva';

const BacktestStarter = ({dispatch}) => {
  return (
    <div />
  );
};

export default connect()(BacktestStarter);
