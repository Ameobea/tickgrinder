//! Component for spawning in new backtester instances

import React from 'react';
import { connect } from 'dva';

const BacktesterSpawner = ({dispatch}) => {
  return (

  );
};

export default connect()(BacktesterSpawner);
