//! Sinks are destinations for ticks.  To create a custom destination for ticks of some kind, you can implement `GenTickSink`
//! for it and export it here.

use tickgrinder_util::transport::tickstream::generics::GenTickSink;

/// Contains all created tick sinks
pub enum Sinks {

}
